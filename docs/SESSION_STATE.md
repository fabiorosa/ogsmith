# OGsmith — session state

## Current status

Spec suite written (PRODUCT, SCOPE, ARCHITECTURE, DECISIONS, UI_SPEC,
BACKLOG, TESTING, AGENTS, README). Awaiting Fabio's spec checkpoint approval.

No code exists yet. No git repository initialized yet.

## Next step

1. Fabio reviews the spec (name, accent color, template set, scope).
2. On approval: `git init`, then start M0-1 (monorepo scaffold).

## Open decisions for the checkpoint

- Project name: `ogsmith` (npm-free, recommended). Alternatives also free:
  `coverforge`, `cardsmith`, `framecard`.
- Studio accent: calm green `#4cc38a` (deliberate contrast with CaseLane's
  amber). Swappable in one token if rejected.

## Do not

- Start coding before the checkpoint approval.
- Add AI co-author trailers or markers anywhere.
- Introduce any second rendering/preview path.
